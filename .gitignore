node_modules
dist
build
.DS_Store
*.log
*.tsbuildinfo
.env
.env.local
coverage
.turbo
.vite
.pnpm-store

# secrets — never commit
*.pem
id_ed25519
id_rsa
id_ecdsa
known_hosts
vapid.json

# terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.auto.tfvars
!*.tfvars.example
crash.log
# *.tfplan files contain a snapshot of the entire Terraform plan including
# the rendered values of every variable — that means OAuth secrets, billing
# account IDs, anything sensitive in secrets.auto.tfvars ends up in the
# plan file. NEVER commit these.
*.tfplan
*.tfplan.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
